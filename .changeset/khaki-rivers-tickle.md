---
"sitectl": minor
---

Add a built-in `Manage caddy` flow for installing Caddy, managing site configs,
deploying/removing remote Caddy site files, listing remote certificate paths,
and uninstalling Caddy. Also split the generic base bootstrap from the
nginx-specific setup flow and simplify the README structure.
